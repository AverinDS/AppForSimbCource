<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlack</item>
        <item name="colorPrimaryDark">@color/colorBlack</item>
        <item name="colorAccent">@color/colorBlack</item>
    </style>
    
    <style name="Switch_style" parent="AlertDialog.AppCompat.Light">
        <item name="colorControlActivated">@color/colorGreen</item>
        <item name="colorSwitchThumbNormal">@color/colorGreenLight</item>
        <item name="android:colorForeground">@color/colorGreenLight</item>
        <item name="trackTint">@color/colorGreenLightAlpha</item>
    </style>

</resources>
